import {connect} from "react-redux";
import PhotoGallery from "./photoGallerry";


let mapStateToProps = (state) => {
    return {

    };
};

let mapDispatchToProps = (dispatch) => {
    return {

    };

};

export default connect(mapStateToProps, mapDispatchToProps)(PhotoGallery);