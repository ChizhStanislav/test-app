import React from "react";
import s from './Title.module.scss';


const Title = () => {
    return (
        <div className={s.cover}>
            <h2>НОВОСТИ</h2>
        </div>

    );
};

export default Title;